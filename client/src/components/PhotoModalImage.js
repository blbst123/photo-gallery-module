import React from 'react';
import PropTypes from 'prop-types';

export default class PhotoModalImage extends React.Component {
  constructor(props) {
    super(props);
    this;
  }

  render() {
    return (
      <div className="photo-modal__image">
      </div>
    );
  }
}

PhotoModalImage.propTypes = {
  // photos: PropTypes.object.isRequired
};
